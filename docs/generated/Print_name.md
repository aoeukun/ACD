# Documentation for `Print_name.py`


Here's a high-quality Google-style docstring for the code you provided:
```python
"""
Asks the user for their name and prints a greeting message.

Args:
    name (str): The user's name.

Returns:
    str: A greeting message that includes the user's name.

Example:
    >>> name = input("What is your name? ")
    What is your name? John
    >>> print("Hello, " + name + "!")
    Hello, John!
"""
```
In this docstring, we provide a concise summary of what the function does (i.e., it asks the user for their name and prints a greeting message), describe the arguments that the function takes (in this case, just one argument called `name` with type `str`), explain what the return value is (a string containing a greeting message), include an example usage of the function (i.e., asking the user for their name and printing the resulting greeting message), and use proper indentation and formatting to make the docstring easy to read and understand.

